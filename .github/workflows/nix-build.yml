name: Build flake artefacts
on: [push]
jobs:
  build:
    runs-on: self-hosted
    steps:
      - name: Check out repository code
        uses: actions/checkout@v3
      - name: Build default output
        run: "nix build ."
